# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Runtime data (bind-mount targets)
data/
/couchdb-data/
/backups/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local dev config (machine-specific paths and models)
config.local.yaml

# Local deploy config (real NAS host — see deploy.env.example)
.deploy.env
