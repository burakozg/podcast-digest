# Copy to .deploy.env and fill in your real values. .deploy.env is
# git-ignored — never commit it. qnap/deploy.sh and qnap/restore-nas.sh
# auto-source it if present, so these never need to be exported by hand
# each session.

NAS_HOST=deploy@nas.local
NAS_SSH_PORT=22
